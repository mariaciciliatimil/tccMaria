import React from 'react'; import { Navigate } from 'react-router-dom'; function getUser(){ try{ return JSON.parse(localStorage.getItem('user')||'null') }catch{return null} } export default function Protected({roles=[],children}){ const user=getUser(); if(!user) return <Navigate to='/' replace/>; if(roles.length && !roles.includes(user.role)) return <Navigate to='/' replace/>; return children }
